name: Reproduce Flaky Tests

on: [push, pull_request]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Set up JDK 8
        uses: actions/setup-java@v2
        with:
          distribution: 'adopt'
          java-version: '8'
          
      # - name: maven clean install 
      #   run: mvn clean install -Dcheckstyle.skip -Drat.skip -DskipTests -U
        
      # - name: Compile Module
      #   run: mvn test-compile -am -Dcheckstyle.skip -Drat.skip -U
      - name: Check
        uses: actions/checkout@v3
        with:
          ref: 2b0ee2d3db43904af6e3409eacbfd31500d2432b
  
      - name: Test with Maven 
        run: | 
              for i in {1..5}; do mvn test -Dtest=Apache.Ignite.Core.Tests.Binary.TypeNameParserTest -DfailIfNoTests=false  -Drat.skip -fae; done#>>test_log_DOXIATOOLS-64.txt || echo "Test iteration $i failed">>test_log_DOXIATOOLS-64.txt; done
              
      # - name: Upload Test Log 
      #   if: always() 
      #   uses: actions/upload-artifact@v2 
      #   with: 
      #     name: test_log_DOXIATOOLS-64
      #     path: test_log_DOXIATOOLS-64.txt
